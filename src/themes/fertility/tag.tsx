import FertilityCategory from './category'
import type { ThemeArchiveProps } from '@/types/theme'

export default function FertilityTag(props: ThemeArchiveProps) {
  return <FertilityCategory {...props} />
}
